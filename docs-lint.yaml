docs_dir: docs
assets_dir: assets
summary: SUMMARY.md

checks:
  warn_only:
    - title-mismatch
    - absolute-links
    # The root README is linked by the docs sidebar product title, not by SUMMARY.md.
    - summary-sync
